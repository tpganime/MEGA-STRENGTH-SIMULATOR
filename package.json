{
  "name": "mega-strength-simulator-hub",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "19.0.0",
    "react-dom": "19.0.0",
    "lucide-react": "^0.460.0",
    "@google/genai": "^0.3.0",
    "@supabase/supabase-js": "^2.46.1"
  },
  "scripts": {
    "dev": "echo 'No local dev server required. Open index.html in a browser.'",
    "build": "mkdir -p dist && cp index.html index.tsx App.tsx types.ts metadata.json vercel.json dist/ && cp -r components services dist/ 2>/dev/null || true"
  }
}